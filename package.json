{
  "name": "@lifull/sesame-keeper",
  "version": "0.0.1",
  "description": "rails-like credential management mechanism.",
  "keywords": [
    "credentials",
    "secrets",
    "env",
    "encrypt",
    "decrypt"
  ],
  "main": "index.js",
  "scripts": {
    "test": "mocha test/**/*-test.js",
    "mocha": "mocha",
    "example": "cd example && npm ci && git init . && npx sesame setup && node app.js"
  },
  "bin": {
    "sesame": "commands/index.js",
    "sesame_diff": "commands/diff.js"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "author": "NakajimaTakuya@lifull.com",
  "license": "Apache-2.0",
  "dependencies": {
    "tmp": "^0.2.1",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "mocha": "^10.3.0"
  }
}
